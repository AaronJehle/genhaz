% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_genhaz.R
\name{fit_genhaz}
\alias{fit_genhaz}
\title{Fit a penalized general hazard model}
\usage{
fit_genhaz(
  surv,
  formula,
  data,
  n_knots = 8,
  lambda = 0,
  model_type = "GH",
  control = list(trace = 0, x.tol = 1e-12, rel.tol = 1e-12, max_iter = 100),
  profile = FALSE,
  lcv_method = c("full", "approx", "optimize"),
  lambda_surv = FALSE,
  init = NULL,
  interval = c(0, 10),
  nquad = 25,
  tol_LCV = 0.001,
  knots = NULL,
  reKnot = 1,
  limit_to_95 = TRUE,
  timeIt = FALSE
)
}
\arguments{
\item{surv}{A \code{Surv} object (from the \strong{survival} package). Right-
censored (\code{Surv(time, event)}), left-truncated and right-censored
(\code{Surv(start, stop, event)}), and interval-censored
(\code{Surv(t1, t2, type = "interval2")}) data are all supported.}

\item{formula}{A one-sided formula specifying the covariate terms, e.g.
\code{~ x + z}. An intercept is added internally; do not include one here.}

\item{data}{A data frame containing all variables referenced in \code{formula}.}

\item{n_knots}{Total number of knots for the baseline log-hazard spline,
including the two boundary knots. Default 8.}

\item{lambda}{Initial (or fixed) smoothing parameter. Ignored when
\code{profile = TRUE}. Default 0.}

\item{model_type}{Character vector of length 1 or \code{ncol(X)} selecting the
model class per covariate: \code{"GH"} (general hazard, default), \code{"PH"}
(proportional hazards), \code{"AFT"} (accelerated failure time), or \code{"AH"}
(additive hazards).}

\item{control}{List of optimisation controls for \code{\link[=nlminb]{nlminb()}}: \code{trace},
\code{x.tol}, \code{rel.tol}, \code{max_iter}.}

\item{profile}{Logical. If \code{TRUE}, optimise \code{lambda} via LCV before
returning the final fit. Default \code{FALSE}.}

\item{lcv_method}{Character string passed to \code{\link[=genhaz_work]{genhaz_work()}} when
\code{profile = TRUE}. Selects the strategy for optimising \code{log(lambda)}:
\code{"full"} (default) uses the full third-derivative LCV gradient,
\code{"approx"} uses the faster first-order approximation, and \code{"optimize"}
minimises LCV directly without gradient information. Ignored when
\code{profile = FALSE}.}

\item{lambda_surv}{Logical. If \code{TRUE}, initialise \code{lambda} from a
\strong{survPen} proportional hazard fit. Requires \strong{survPen} to be
installed. Default \code{FALSE}.}

\item{init}{Optional numeric vector of starting values. If \code{NULL}, zeros
are used.}

\item{interval}{Length-2 numeric vector giving the search interval for
\code{log(lambda)} during LCV optimisation. Default \code{c(0, 10)}.}

\item{nquad}{Number of Gauss-Legendre quadrature points. Default 25.}

\item{tol_LCV}{Convergence tolerance for LCV optimisation. Default \code{1e-3}.}

\item{knots}{Optional numeric vector of knot positions on the log-time
scale. If \code{NULL} (default), knots are placed automatically using
\code{\link[=knot_pattern]{knot_pattern()}}.}

\item{reKnot}{Non-negative integer. After the initial fit, re-position
knots \code{abs(reKnot)} times using the AFT-adjusted times and refit.
Default \code{1} (one re-knot pass). Set to \code{0} to skip.}

\item{limit_to_95}{Logical passed to \code{\link[=knot_pattern]{knot_pattern()}}. Default \code{TRUE}.}

\item{timeIt}{Logical. If \code{TRUE}, print the wall-clock fitting time.
Default \code{FALSE}.}
}
\value{
A list of class \code{"genhaz_fit"} containing (among others):
\describe{
\item{\code{par}}{Named vector of parameter estimates.}
\item{\code{se}}{Standard errors.}
\item{\code{z}}{Wald z-statistics.}
\item{\code{p_values}}{Two-sided p-values.}
\item{\code{var}}{Estimated covariance matrix of \code{par}.}
\item{\code{AIC}}{Akaike information criterion.}
\item{\code{edf}}{Effective degrees of freedom.}
\item{\code{lambda}}{Smoothing parameter used.}
\item{\code{knots}}{Knot vector on the log-time scale.}
\item{\code{Z}}{Spline projection matrix.}
}
}
\description{
High-level interface for fitting a penalised cubic regression spline
general hazard (GH) model. Wraps \code{\link[=genhaz_work]{genhaz_work()}} with automatic knot
placement, design matrix construction, and optional profiling of the
smoothing parameter \code{lambda} via the modified LCV criterion.
}
\examples{
\dontrun{
library(survival)
set.seed(42)
dat <- sim_szenario(szenario = 1, beta1 = 0.5, beta2 = 0.5, n = 300)
fit <- fit_genhaz(Surv(dat$time, dat$event), ~ X, data = dat,
                  model_type = "GH", profile = TRUE, n_knots = 6,
                  tol_LCV = 0.01)
fit$par
fit$se
}
}
