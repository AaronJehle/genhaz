% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/post.R
\name{post}
\alias{post}
\title{Post-estimation from a fitted general hazard model}
\usage{
post(fit, X, time, res, event = NULL)
}
\arguments{
\item{fit}{A fitted model object returned by \code{\link[=fit_genhaz]{fit_genhaz()}}.}

\item{X}{Covariate vector or matrix for prediction. If a vector, it is
treated as a single covariate pattern and replicated across all \code{time}
points. If a single-row matrix and \code{length(time) > 1}, the row is
replicated. If \code{NULL}, all covariates are set to zero.}

\item{time}{Numeric vector of time points at which to evaluate the quantity.}

\item{res}{Character string; the quantity to return. Supports all non-fitting
options of \code{\link[=genhaz_work]{genhaz_work()}}: \code{"h"}, \code{"H"}, \code{"log_h"}, \code{"gradient_H"},
\code{"gradient_h"}, etc.}

\item{event}{Optional event indicator vector, required for some \code{res} values
(e.g. \code{"scores"}).}
}
\value{
Numeric vector or matrix of the requested quantity evaluated at
each \verb{(time, X)} combination.
}
\description{
Evaluates model quantities (hazard, cumulative hazard, survival, gradients,
etc.) at arbitrary covariate patterns and time points, using the parameters
of a fitted model object.
}
\examples{
\dontrun{
library(survival)
set.seed(42)
dat <- sim_szenario(1, beta1 = 0.5, beta2 = 0.5, n = 300)
fit <- fit_genhaz(Surv(dat$time, dat$event), ~X, data = dat,
                  n_knots = 6, profile = TRUE, tol_LCV = 0.01)

t_grid <- seq(0.01, 8, length.out = 200)
h_est  <- post(fit, X = 0, time = t_grid, res = "h")
plot(t_grid, h_est, type = "l", xlab = "Time", ylab = "Hazard")
}
}
