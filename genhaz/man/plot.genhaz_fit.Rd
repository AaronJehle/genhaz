% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generics.R
\name{plot.genhaz_fit}
\alias{plot.genhaz_fit}
\title{Plot estimated curves from a fitted GH model}
\usage{
\method{plot}{genhaz_fit}(
  x,
  newdata,
  times,
  type = c("hazard", "survival", "cumhaz"),
  alpha = 0.05,
  col = NULL,
  lty = 1,
  xlab = "Time",
  ylab = NULL,
  main = NULL,
  legend = TRUE,
  ci = TRUE,
  ...
)
}
\arguments{
\item{x}{A \code{"genhaz_fit"} object from \code{\link[=fit_genhaz]{fit_genhaz()}}.}

\item{newdata}{A \code{data.frame} of covariate values. One row per group.
Set row names for informative legend labels.}

\item{times}{Numeric vector of evaluation time points.}

\item{type}{Quantity to plot: \code{"hazard"} (default), \code{"survival"}, or
\code{"cumhaz"}.}

\item{alpha}{Significance level for confidence bands. Default 0.05.}

\item{col}{Colour vector (recycled over groups). Defaults to \verb{2, 3, 4, ...}
(R's standard colour sequence, skipping black).}

\item{lty}{Line type for point-estimate curves. Default \code{1}.}

\item{xlab}{X-axis label. Default \code{"Time"}.}

\item{ylab}{Y-axis label. Derived from \code{type} when \code{NULL}.}

\item{main}{Plot title. Derived from \code{type} when \code{NULL}.}

\item{legend}{Logical; draw a legend when multiple groups are present?
Default \code{TRUE}.}

\item{ci}{Logical; overlay dashed confidence band lines? Default \code{TRUE}.}

\item{...}{Additional graphical parameters passed to \code{\link[graphics:plot.default]{graphics::plot()}}.}
}
\value{
Invisibly returns the \code{data.frame} produced by
\code{predict.genhaz_fit()}.
}
\description{
Plots hazard, survival, or cumulative hazard curves for one or more
covariate patterns. Each row of \code{newdata} becomes a separate coloured
line; row names serve as legend labels. Confidence bands are drawn as
dashed lines of the same colour.
}
\examples{
\dontrun{
t_grid <- seq(0.01, 8, length.out = 300)

# Hazard (default) for two groups
nd <- data.frame(X = c(0, 1))
rownames(nd) <- c("X = 0", "X = 1")
plot(fit, newdata = nd, times = t_grid)

# Survival for a single group
plot(fit, newdata = data.frame(X = 0), times = t_grid, type = "survival")
}
}
