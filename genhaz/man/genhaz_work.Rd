% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/genhaz_work.R
\name{genhaz_work}
\alias{genhaz_work}
\title{General hazard model workhorse}
\usage{
genhaz_work(
  theta,
  time,
  X,
  knots,
  Z,
  event = NULL,
  lambda = 1,
  res = c("log_h", "gradient_log_h", "hessian_log_h", "h", "gradient_h", "hessian_h",
    "H", "gradient_H", "hessian_H", "negll", "negll_upen", "scores", "gradient_negll",
    "hessian_negll", "hessian_negll_upen", "basis", "edf", "LCV", "dLCV", "dLCV_full",
    "fit", "fit_LCV"),
  model_type = "GH",
  time2 = NULL,
  cens_type = "rc",
  gaussified = TRUE,
  nquad = 25,
  control = list(trace = 0, x.tol = 1e-12, rel.tol = 1e-12, max_iter = 100),
  lcv_method = c("full", "approx", "optimize"),
  interval = c(0, 13),
  tol_LCV = 0.001,
  ...
)
}
\arguments{
\item{theta}{Numeric vector of parameters: intercept, spline coefficients,
then beta1 (AFT-style effects) and beta2 (PH-style effects).}

\item{time}{Numeric vector of observed (or evaluation) times.}

\item{X}{Numeric matrix of covariates (rows = observations).}

\item{knots}{Numeric vector of knot positions on the log-time scale,
including boundary knots.}

\item{Z}{Projection matrix for the spline basis (from \code{\link[=smf_cpp]{smf_cpp()}}).}

\item{event}{Integer event indicator vector (1 = event, 0 = censored).
Required for fitting.}

\item{lambda}{Non-negative smoothing parameter for the spline penalty.}

\item{res}{Character string selecting the quantity to return. One of
\code{"log_h"}, \code{"gradient_log_h"}, \code{"hessian_log_h"}, \code{"h"},
\code{"gradient_h"}, \code{"hessian_h"}, \code{"H"}, \code{"gradient_H"}, \code{"hessian_H"},
\code{"negll"}, \code{"negll_upen"}, \code{"scores"}, \code{"gradient_negll"},
\code{"hessian_negll"}, \code{"hessian_negll_upen"}, \code{"basis"}, \code{"edf"}, \code{"LCV"},
\code{"dLCV"}, \code{"dLCV_full"}, \code{"fit"}, or \code{"fit_LCV"}. The last two fit a
model: \code{"fit"} at a fixed \code{lambda}, \code{"fit_LCV"} after profiling \code{lambda}
via LCV. \code{"dLCV"} returns the first-order analytical derivative of LCV
w.r.t. \code{log(lambda)} (evaluated at the MLE for the given \code{lambda}).
\code{"dLCV_full"} returns the same derivative including the third-derivative
correction term from the unpenalised Hessian's dependence on \code{lambda}
(supports \code{"rc"} and \code{"lt_rc"}; falls back to \code{"dLCV"} for \code{"ic"}).}

\item{model_type}{Character vector specifying the model type for each
covariate: \code{"GH"} (general hazard), \code{"PH"} (proportional hazards,
beta1 = 0), \code{"AFT"} (accelerated failure time, beta2 = 0), or
\code{"AH"} (additive hazards, beta1 = -beta2). If length 1, recycled to all
covariates.}

\item{time2}{Numeric vector for the second time variable. Used as the
truncation time for \code{cens_type = "lt_rc"}, or as the right interval
endpoint for \code{cens_type = "ic"}.}

\item{cens_type}{Censoring type: \code{"rc"} (right-censoring, default),
\code{"lt_rc"} (left-truncation + right-censoring), or \code{"ic"} (interval
censoring).}

\item{gaussified}{Logical; use Gauss-Legendre quadrature for integrating
the cumulative hazard? Default \code{TRUE}. Setting to \code{FALSE} requires
\strong{rstpm2} to be installed.}

\item{nquad}{Number of quadrature points when \code{gaussified = TRUE}.
Default 25.}

\item{control}{List of optimisation control parameters passed to
\code{\link[=nlminb]{nlminb()}}: \code{trace}, \code{x.tol}, \code{rel.tol}, \code{max_iter}.}

\item{lcv_method}{Character string. When \code{res = "fit_LCV"}, selects the
strategy for optimising \code{log(lambda)}. One of:
\describe{
\item{\code{"full"} (default)}{Root-find on the full LCV gradient including
the third-derivative correction (\code{dLCV_full}). Most accurate.}
\item{\code{"approx"}}{Root-find on the first-order LCV gradient (\code{dLCV}),
which ignores the dependence of the Hessian on \code{lambda}. Faster than
\code{"full"}.}
\item{\code{"optimize"}}{Directly minimise LCV without gradient information
via \code{optimize()}. Equivalent to the original behaviour before analytic
gradients were added.}
}
Ignored for all other \code{res} values and falls back to \code{lcv_gradient} for
\code{cens_type = "ic"}.}

\item{interval}{Length-2 numeric vector giving the search interval for
\code{log(lambda)} during LCV optimisation. Default \code{c(0, 13)}.}

\item{tol_LCV}{Convergence tolerance for LCV optimisation. Default \code{1e-3}.}

\item{...}{Additional arguments (currently unused).}
}
\value{
Depends on \code{res}. When \code{res = "fit"} or \code{"fit_LCV"}, returns a list
with components \code{par}, \code{se}, \code{z}, \code{p_values}, \code{AIC}, \code{edf}, \code{lambda},
\code{var}, and further fitting details.
}
\description{
Calculates various quantities from a general hazard (GH) model depending on
the \code{res} argument: negative log-likelihood and its gradient/Hessian, the
cumulative hazard H and its derivatives, effective degrees of freedom, the
LCV criterion, or a fully fitted model object. Should not be called directly
by users; use \code{\link[=fit_genhaz]{fit_genhaz()}} and \code{\link[=post]{post()}} instead.
}
