% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{smf_cpp}
\alias{smf_cpp}
\title{C++ reimplementation of \code{smf}, including derivatives.}
\usage{
smf_cpp(
  x,
  df = 10L,
  knots = NULL,
  intercept = FALSE,
  derivs = 0L,
  Z = NULL,
  survPen_compatible = FALSE
)
}
\arguments{
\item{x}{a vec of values to provide}

\item{df}{a non-negative integer for the degrees of freedom (ignored if
the knots are specified)}

\item{knots}{a vec of knots (defaults to NULL)}

\item{intercept}{a bool for whether to include the intercept}

\item{derivs}{a non-negative integer for the derivative (default 0)}

\item{Z}{optional pre-computed QR projection matrix (arma::mat). If
provided, overrides the internally computed centering matrix.}

\item{survPen_compatible}{a bool for whether to use \code{unique(x)} for
determining the knots if the \code{knots} are not specified}
}
\value{
an \code{Rcpp::NumericMatrix} with attributes \code{class="smf_cpp"},
\code{pen} for the penalty matrix, \code{knots} for the knots, \code{derivs} for the order of
derivatives, and \code{intercept} for a bool/logical for whether the intercept is
included. If there is no intercept, then there will also be a \code{Z} attribute
which is the QR-based projection matrix.
}
\description{
The re-implementation uses the same algorithm but uses
different defaults. Note that the inclusion of an
intercept is the same as \code{crs_cpp}, while no intercept uses a projection
based on a QR decomposition.
For the changes in defaults: the function here assumes that
the intercept is false (as per \code{splines::ns}); and when the knots are
not specified, the function defaults to using \code{x} rather than \code{unique(x)}
for the quantiles. The \code{survPen} implementation for \code{smf} assumes no
intercept unless there is a \code{by} variable. A \code{by} variable \code{x2} would
be implemented here by an interaction, such as
\code{smf_cpp(x,knots,intercept=TRUE):x2}; see the examples.
}
\examples{
x     <- seq(0, 1, length.out = 11)
knots <- c(1, 3, 5, 7) / 10
smf1  <- smf_cpp(x, knots = knots)
dim(smf1)
# Check that Z attribute is available without intercept:
!is.null(attr(smf1, "Z"))

## Derivative check via finite differences (knots must be explicit)
smfD <- function(t, knots, eps = 1e-5) {
  (smf_cpp(t + eps, knots = knots) - smf_cpp(t - eps, knots = knots)) / (2 * eps)
}
x2 <- seq(0, 1, length.out = 101)
max(abs(smf_cpp(x2, knots = knots, derivs = 1) - smfD(x2, knots = knots)))

## Comparison with survPen::smooth.cons (requires survPen):
\dontrun{
library(survPen)
smf2 <- smooth.cons("x", knots = list(knots), df = 4, option = "smf",
                    data.spec = data.frame(x = x), name = "smf(time)")
range(smf1 - smf2$X)
range(attr(smf1, "pen") - smf2$pen[[1]])
}
}
