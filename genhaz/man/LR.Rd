% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/inference.R
\name{LR}
\alias{LR}
\title{Likelihood ratio test between nested GH models}
\usage{
LR(fit_nested, fit_general)
}
\arguments{
\item{fit_nested}{Fitted model object (the restricted model).}

\item{fit_general}{Fitted model object (the general model).}
}
\value{
Named numeric vector with \code{LR-statistic} and \code{p_value}.
}
\description{
Computes a chi-squared likelihood ratio test statistic comparing a nested
(restricted) model against a more general model.
}
\examples{
\dontrun{
fit_ph <- fit_genhaz(Surv(dat$time, dat$event), ~X,
                     data = dat, model_type = "PH", n_knots = 6)
fit_gh <- fit_genhaz(Surv(dat$time, dat$event), ~X,
                     data = dat, model_type = "GH", n_knots = 6)
LR(fit_ph, fit_gh)
}
}
