% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/inference.R
\name{confint.genhaz_fit}
\alias{confint.genhaz_fit}
\title{Wald confidence intervals for model parameters}
\usage{
\method{confint}{genhaz_fit}(object, parm, level = 0.95, diff = FALSE, ...)
}
\arguments{
\item{object}{A \code{"genhaz_fit"} object from \code{\link[=fit_genhaz]{fit_genhaz()}}.}

\item{parm}{Character vector of parameter names (must be in
\code{object$parnames}). If missing, all parameters are used. When
\code{diff = TRUE}, exactly two names must be supplied.}

\item{level}{Confidence level. Default \code{0.95}.}

\item{diff}{Logical. If \code{TRUE}, return a single interval for
\code{parm[1] - parm[2]} via the delta method. Default \code{FALSE}.}

\item{...}{Currently unused.}
}
\value{
A matrix with one row per parameter (or a single row for the
difference) and two columns giving the lower and upper bounds.
}
\description{
Computes Wald confidence intervals for the parameters of a fitted GH model,
following the \code{\link[stats:confint]{stats::confint()}} convention. With \code{diff = TRUE} it instead
returns a delta-method interval for the difference of two parameters,
accounting for their covariance.
}
\examples{
\dontrun{
confint(fit)                                       # all parameters
confint(fit, "beta2_X")                            # one parameter
confint(fit, c("beta1_X", "beta2_X"), diff = TRUE) # difference
}
}
