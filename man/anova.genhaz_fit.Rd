% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/inference.R
\name{anova.genhaz_fit}
\alias{anova.genhaz_fit}
\title{Likelihood ratio tests between nested GH models}
\usage{
\method{anova}{genhaz_fit}(object, ...)
}
\arguments{
\item{object}{A \code{"genhaz_fit"} object from \code{\link[=fit_genhaz]{fit_genhaz()}} (the first, usually
most restricted, model).}

\item{...}{One or more further \code{"genhaz_fit"} objects to compare against
\code{object}. Non-\code{"genhaz_fit"} arguments are ignored.}
}
\value{
An analysis-of-deviance table of class \code{c("anova", "data.frame")}
with one row per model and columns \code{Df} (model degrees of freedom),
\code{LogLik}, \code{Chisq}, \verb{Chi Df}, and \verb{Pr(>Chisq)} (the comparison columns are
\code{NA} for the first model).
}
\description{
Compares two or more nested \code{"genhaz_fit"} models with chi-squared
likelihood-ratio tests, in the style of \code{\link[stats:anova]{stats::anova()}} for \code{lm}/\code{glm}
objects. Models are compared sequentially in the order supplied, so list the
most restricted model first.
}
\examples{
\dontrun{
fit_ph <- fit_genhaz(Surv(dat$time, dat$event), ~X,
                     data = dat, model_type = "PH", n_knots = 6)
fit_gh <- fit_genhaz(Surv(dat$time, dat$event), ~X,
                     data = dat, model_type = "GH", n_knots = 6)
anova(fit_ph, fit_gh)
}
}
