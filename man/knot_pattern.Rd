% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/knot_pattern.R
\name{knot_pattern}
\alias{knot_pattern}
\title{Compute knot positions from event times}
\usage{
knot_pattern(time, event, n_knots, limit_to_95 = TRUE)
}
\arguments{
\item{time}{Numeric vector of observed times.}

\item{event}{Integer vector of event indicators (1 = event, 0 = censored),
same length as \code{time}.}

\item{n_knots}{Total number of knots, including the two boundary knots.}

\item{limit_to_95}{Logical. If \code{TRUE} (default) the upper boundary knot
is placed at the 95 \% quantile; if \code{FALSE} it is placed at the maximum
observed event time.}
}
\value{
Named numeric vector of length \code{n_knots} with knot positions on the
\emph{log-time} scale, as required by \code{\link[=genhaz_work]{genhaz_work()}} and \code{\link[=fit_genhaz]{fit_genhaz()}}.
}
\description{
Places \code{n_knots} equally spaced (in probability) quantiles of the
log-event-times. Boundary knots are placed at the 5 \% and, optionally,
the 95 \% or 100 \% quantile of the event-time distribution.
}
\examples{
set.seed(1)
t  <- rexp(200)
ev <- rbinom(200, 1, 0.8)
knot_pattern(t, ev, n_knots = 6)
}
