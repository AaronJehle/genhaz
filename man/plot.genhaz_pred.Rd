% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generics.R
\name{plot.genhaz_pred}
\alias{plot.genhaz_pred}
\title{Plot a genhaz prediction}
\usage{
\method{plot}{genhaz_pred}(
  x,
  col = NULL,
  lty = 1,
  xlab = "Time",
  ylab = NULL,
  main = NULL,
  legend = TRUE,
  ci = TRUE,
  ...
)
}
\arguments{
\item{x}{A \code{"genhaz_pred"} object returned by \code{\link[=predict.genhaz_fit]{predict.genhaz_fit()}}.}

\item{col}{Colour vector (recycled over groups). Defaults to \verb{2, 3, 4, ...}
(R's standard colour sequence, skipping black).}

\item{lty}{Line type for point-estimate curves. Default \code{1}.}

\item{xlab}{X-axis label. Default \code{"Time"}.}

\item{ylab}{Y-axis label. Derived from the prediction type when \code{NULL}.}

\item{main}{Plot title. Derived from the prediction type when \code{NULL}.}

\item{legend}{Logical; draw a legend when multiple groups are present?
Default \code{TRUE}.}

\item{ci}{Logical; overlay dashed confidence band lines? Default \code{TRUE}.}

\item{...}{Additional graphical parameters passed to \code{\link[graphics:plot.default]{graphics::plot()}}.}
}
\value{
Invisibly returns \code{x}.
}
\description{
Plots the output of \code{\link[=predict.genhaz_fit]{predict.genhaz_fit()}} — any of the eight prediction
types. Each distinct covariate pattern becomes a coloured line; confidence
bands are drawn as dashed lines of the same colour. \code{ylim} is always
derived from the full range of CI bands plus the point estimate, so nothing
is clipped. Axis labels and title are set automatically from the prediction
type and can be overridden via \code{ylab} / \code{main}.
}
\examples{
\dontrun{
nd <- data.frame(X = c(0, 1))
rownames(nd) <- c("X = 0", "X = 1")
t_grid <- seq(0.01, 8, length.out = 200)

pred <- predict(fit, newdata = nd, times = t_grid, type = "hazard_ratio")
plot(pred, col = "purple")
abline(h = 1, lty = 2)
}
}
